# Project2026V1
A1**.py is the file that generates the dataset for LoRA fine-tuning procedure. The number of the video is defined by yourself.
A2**.py is the file that generates the dataset for reinforcement learning (RL) procedure.
A3**.py is the file to validate whether the generationg of the RL dataset is successful.
A4**.py is the file used for LoRA training procedure.
A7**.py is used to eval the effect of the LoRA training method.
A10**.py is used to eval the effect of the GSPO training method.

Only the preliminary trainig process of LoRA is shown here. The complete training code will be uploaded shortly after the paper is accepted.
